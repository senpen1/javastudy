public class Main {
    public static void main(String[]args){
        Supervilan vilan = new Supervilan();//Supervilanクラスのインスタンスを生成する
        vilan.name = "ドクター・エビル";//インスタンスに名前を設定

        SuperHero hero = new SuperHero();//SuperHeroクラスのインスタンスを生成する
        hero.name = "スーパーマン";//インスタンスに名前を設定
        
        vilan .evilPlan();//邪悪な計画を実行するメソッドを呼び出す。






        /*
        Hero h1 = new Hero("ミナト");//インスタンスを生成
        h1.hp = 100;//初期値をセット
        Hero h2 = new Hero("わんこ");//ここでもインスタンスを生成
        h2.hp = 100;//また初期値
        Wizard w = new Wizard();//ここでもインスタンスを生成
        w.name = "スガワラ";//初期値をセット
        w.hp = 50;//初期値をセット
        Cleric c = new Cleric();//インスタンスを生成
        c.name = "レナ";//初期値をセット
        c.hp =50;
        w.heal(h1);//ヒールメソッドを呼び出し、h1を回復
        w.heal(h2);
        w.heal(h2);
        */
    }
}





//hero.name = "勇者";//名前を設定
        //hero.hp = 100;//hpを設定
        //hero.ataack();//攻撃メソッドを呼び出す。
